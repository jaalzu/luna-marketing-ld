

<section class="relative py-20  overflow-hidden">
  <!-- Background decoration (más sutil con blanco) -->
  <div class="absolute inset-0 opacity-30">
    <div class="absolute top-20 left-10 w-72 h-72  rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 right-10 w-96 h-96  rounded-full blur-3xl"></div>
  </div>

  <div class="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Motivational phrase -->
    <div class="mb-8">
      <p class="text-lg md:text-xl text-gray-700 font-light leading-relaxed mb-12">
        "El conocimiento es poder, pero el conocimiento aplicado es 
        <span class="font-semibold text-transparent bg-clip-text bg-gradient-to-r from-[#3b82f6] to-[#1e40af]">
          transformación
        </span>"
      </p>
    </div>

    <!-- Main heading -->
    <h3 class="text-4xl md:text-6xl font-bold text-black mb-6 leading-tight">
      Descarga tu 
      <span class="relative inline-block">
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-[#3b82f6] to-[#1e40af]">
          Ebook Gratuito
        </span>
        <div class="absolute -bottom-1 left-0 right-0 h-1 bg-gradient-to-r from-[#3b82f6] to-[#1e40af] opacity-60"></div>
      </span>
    </h3>

    <!-- Subtitle -->
    <p class="text-xl md:text-2xl text-gray-600 mb-10 max-w-3xl mx-auto leading-relaxed">
      "Los 7 Secretos que Todo Emprendedor Debe Conocer para 
      <strong class="text-black">Escalar su Negocio</strong> en 2024"
    </p>

    <!-- CTA Form -->
    <div class="max-w-md mx-auto mt-10">
      <form class="space-y-3">
        <div class="relative">
          <input 
            type="email" 
            placeholder="example@gmail.com" 
            class="w-full px-6 py-4 rounded-full bg-white border border-gray-300 text-black placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-[#00ffe0]"
            required
          />
        </div>
        
        <button 
          type="submit"
          class="w-full py-4 px-8 rounded-full font-semibold text-white text-lg transition-all duration-300 hover:scale-105 hover:shadow-xl transform bg-primary"
        >
          Descargar Ebook GRATIS
        </button>
      </form>

      <!-- Trust indicators -->
     
<div class="mt-6 flex items-center justify-center space-x-6 text-sm text-gray-500">
  <div class="flex items-center space-x-2">
    <div class="w-3 h-3 bg-primary rounded-full"></div>
    <span>100% Seguro</span>
  </div>
  
  <div class="flex items-center space-x-2">
    <div class="w-3 h-3 bg-primary rounded-full"></div>
    <span>Sin Spam</span>
  </div>
  
  <div class="flex items-center space-x-2">
    <div class="w-3 h-3 bg-primary rounded-full"></div>
    <span>Descarga Instantánea</span>
  </div>
</div>

      <!-- Social proof -->
      <p class="mt-8 text-gray-500 text-sm">
        <strong class="text-black">+1000</strong> emprendedores ya lo descargaron
      </p>
    </div>
  </div>
</section>
