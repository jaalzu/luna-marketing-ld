---
// Testimonials.astro
const testimonials = [
  {
    id: 1,
    name: "María González",
    image: "https://images.unsplash.com/photo-1494790108755-2616b612b786?w=64&h=64&fit=crop&crop=face&auto=format",
    comment: "Increíble servicio, superó todas mis expectativas. El equipo es muy profesional.",
    duration: "2:34"
  },
  {
    id: 2,
    name: "Carlos Ruiz",
    image: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=64&h=64&fit=crop&crop=face&auto=format",
    comment: "La mejor decisión que tomamos fue trabajar con ellos. Los resultados hablan por sí solos.",
    duration: "1:45"
  },
  {
    id: 3,
    name: "Ana Martín",
    image: "https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=64&h=64&fit=crop&crop=face&auto=format",
    comment: "Transformaron completamente nuestro negocio. Su enfoque innovador marcó la diferencia.",
    duration: "3:12"
  },
  {
    id: 4,
    name: "Jorge López",
    image: "https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=64&h=64&fit=crop&crop=face&auto=format",
    comment: "Excelente atención al cliente y resultados excepcionales. Los recomiendo al 100%.",
    duration: "2:18"
  }
];
---

<section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 mt-11 mb-12 py-16">
  <!-- Header -->
  <div class="text-center mb-12">
    <h3 class="text-2xl md:text-3xl font-bold text-gray-900">
      Experiencias de clientes
    </h3>
  </div>

  <!-- Testimonials Grid -->
  <div class="grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-16">
    {testimonials.map((testimonial) => (
      <div class="flex flex-col items-center text-center">
        <!-- Video Container (Smaller) -->
        <div class="relative bg-gray-900 rounded-lg overflow-hidden w-full max-w-sm aspect-video mb-4">
          <div class="absolute inset-0 flex items-center justify-center">
            <!-- Minimalist Play Button -->
            <button 
              class="group w-16 h-16 flex items-center justify-center hover:scale-110 transition-transform duration-200"
              aria-label={`Reproducir testimonial de ${testimonial.name}`}
            >
              <svg class="w-16 h-16 text-white opacity-90 group-hover:opacity-100 transition-opacity" fill="currentColor" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10" fill="white" fill-opacity="0.2"/>
                <path d="M10 8.5v7l6-3.5z"/>
              </svg>
            </button>
          </div>
          <!-- Video Duration -->
          <div class="absolute bottom-3 right-3 bg-black bg-opacity-60 text-white px-2 py-1 rounded text-xs font-medium">
            {testimonial.duration}
          </div>
        </div>
        
        <!-- Name -->
        <h4 class="font-semibold text-gray-900 text-lg mb-2">
          {testimonial.name}
        </h4>
        
        <!-- Quote -->
        <blockquote class="text-gray-600 text-sm leading-relaxed max-w-md">
          "{testimonial.comment}"
        </blockquote>
      </div>
    ))}
  </div>
</section>

<script>
  // Funcionalidad para los botones de play
  document.querySelectorAll('button[aria-label^="Reproducir"]').forEach(button => {
    button.addEventListener('click', () => {
      console.log('Reproducir video testimonio');
      // Aquí puedes agregar la lógica para reproducir el video
    });
  });
</script>